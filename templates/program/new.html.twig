{% extends "base.html.twig" %}

{% block body %}
	{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
	{{ form_widget(form) }}
	<button type="submit">Ajouter ce nouveau programme</button>
	{{ form_end(form) }}
{% endblock %}
